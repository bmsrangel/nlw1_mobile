import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:mobile/app/modules/points/points_page.dart';

main() {
  testWidgets('PointsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PointsPage(title: 'Points')));
    //  final titleFinder = find.text('Points');
    //  expect(titleFinder, findsOneWidget);
  });
}
